import React from 'react';

const UniqueHostComponent = ({ title }) => {
  return (
    <div>
      <h2>{title}</h2>
      <p>This is a unique component for the Host application.</p>
    </div>
  );
};

export default UniqueHostComponent;
