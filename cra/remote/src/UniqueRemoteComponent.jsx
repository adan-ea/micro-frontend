import React from 'react';

const UniqueRemoteComponent = ({ title }) => {
  return (
    <div>
      <h2>{title}</h2>
      <p>This is a unique component for the Remote application.</p>
    </div>
  );
};

export default UniqueRemoteComponent;
